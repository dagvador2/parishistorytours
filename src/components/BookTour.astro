---
import BookingWizard from "./BookTour/BookingWizard";
import { en } from '../i18n/translations/en';
import { fr } from '../i18n/translations/fr';

interface Props {
  defaultTour?: 'left-bank' | 'right-bank';
  tourFilter?: string; // legacy prop alias
}

const { defaultTour, tourFilter } = Astro.props;
const resolvedTour = defaultTour || tourFilter;

const lang = Astro.locals.lang;
const allTranslations = { en, fr };
const bookingT = allTranslations[lang].booking;
---

{/* Section Book your tour */}
<div id="book-tour" class="mb-16">
	<BookingWizard client:visible translations={bookingT} lang={lang} defaultTour={resolvedTour} />
</div>
